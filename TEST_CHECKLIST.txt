========================================================
  BIZMATCH — NEW FEATURES TEST CHECKLIST
========================================================
Backend: https://zooming-surprise-production.up.railway.app
Test by running: npx expo start --web (or Expo Go)

Legend:  [ ] = not tested   [x] = passed   [!] = failed

--------------------------------------------------------
1. DESIGN & UI
--------------------------------------------------------

[x] 1.1 Custom navbar icons
      - Open the app and look at the bottom tab bar
      - Expected: icons are Ionicons (compass, chat bubbles, folder, person circle)
      - Expected: active tab shows filled icon, inactive shows outline

[x] 1.2 App logo in header
      - Navigate between any of the 4 main tabs
      - Expected: BizMatch logo (two overlapping circles) + "BizMatch" wordmark
        appear at the top of every tab screen
      - Expected: consistent across Discover, Matches, Projects, Profile

[ ] 1.3 Notification badge on Matches tab
      - Have at least one match with no messages yet
      - Expected: red badge with count appears on the Matches tab icon
      - Open the Matches tab → navigate back to another tab
      - Expected: badge count updates (goes to 0 if all matches have messages)
      - Open a chat → go back to Matches
      - Expected: blue dot disappears for that conversation, badge count decreases
      FIXED: bell icon removed; badge now clears after opening a chat

--------------------------------------------------------
2. PROFILE PHOTO UPLOAD
--------------------------------------------------------
  BEFORE TESTING: ensure BACKEND_URL is set in Railway backend variables.

[ ] 2.1 Add photo (first time)
      - Go to Profile → Edit Profile
      - Expected: a circular photo area appears at the top of the form
      - Tap it → image library opens
      - Select a photo
      - Expected: photo previews immediately in the circle
      - Expected: small edit badge shows "…" while uploading, then "✎" when done
      - Save profile → go back to Profile screen
      - Expected: photo appears on your profile
      FIXED: profile API now returns photo_url from users table

[ ] 2.2 Change photo
      - Go to Edit Profile again → tap the photo circle
      - Select a different photo
      - Expected: new photo replaces the old one

[ ] 2.3 Photo appears in swipe feed
      - Log in as a second account
      - Go to Discover tab
      - Expected: the first account's photo appears on their swipe card

--------------------------------------------------------
3. CHANGE ROLE
--------------------------------------------------------

[x] 3.1 Change role from Account Settings
      - Go to Profile → Account Settings
      - Expected: a "Change Role" card shows your current role
      - Tap "Change Role"
      - Expected: role selection screen appears (Entrepreneur / Investor cards)
      - Select the opposite role → tap Continue
      - Expected: navigates to profile form with blank fields (old role data cleared)
      - Fill in new profile details → Save
      - Expected: no duplicate key error
      FIXED: upsert on backend + blank form defaults on role change

[x] 3.2 Feed reflects new role
      - After changing role, go to Discover tab
      - Expected: feed now shows profiles of the opposite role

[x] 3.3 Role persists after logout
      - Log out and log back in
      - Expected: new role is still active

--------------------------------------------------------
4. MOBILE RESPONSIVENESS
--------------------------------------------------------

[ ] 4.1 Test on iPhone 15 (393pt wide) or simulator
      - Run on iOS/Android (Expo Go)
      - Navigate through all screens
      - Expected: no clipped text, no overflowing cards, no broken spacing
      - Check: swipe cards, match modal, chat input, profile form, project form
      FIXED: swipe card width = min(340, 90% screen), modal width = min(300, 85% screen)
