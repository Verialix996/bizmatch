========================================================
  BIZMATCH — UPCOMING FEATURES
========================================================
Backend: https://zooming-surprise-production.up.railway.app
Test by running: npx expo start --web (or Expo Go)

--------------------------------------------------------
1. PROFILE
--------------------------------------------------------

[ ] 1.1 Upload profile photo
      BEFORE TESTING: set BACKEND_URL = https://zooming-surprise-production.up.railway.app
      in Railway backend variables (needed to build the photo URL)
      - Go to Edit Profile → tap the photo circle
      - Select image from library
      - Expected: photo previews immediately, uploads in background
      - Expected: photo appears on profile and in the swipe feed

[ ] 1.2 Change role (entrepreneur ↔ investor)
      - Profile → Account Settings → tap "Change Role"
      - Select new role → Continue
      - Expected: feed changes to show opposite role

--------------------------------------------------------
2. DESIGN & UI
--------------------------------------------------------

[ ] 2.1 Custom navbar icons
      - Expected: tab bar shows Ionicons (compass, chat, folder, person)
        instead of emoji

[ ] 2.2 App logo in header
      - Expected: BizMatch logo + wordmark visible at top of every main screen

[ ] 2.3 Notification badge on Matches tab
      - Expected: badge shows count of new matches (matches with no messages yet)
      - Badge disappears once you open the Matches tab

[ ] 2.4 Mobile responsiveness
      - Test on iOS/Android device or simulator
      - Expected: no clipped text, overflowing cards, or broken spacing
